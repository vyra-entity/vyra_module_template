name: "{{ module_name }}"
display_name: "{{ module_display_name }}"
description: "{{ module_description }}"
version: "{{ module_version }}"
author: "{{ author_name }}"
author_email: "{{ author_email }}"
template:
{% for t in module_template.split(',') %}
  - "{{ t.strip() }}"
{% endfor %}
uuid: ""
