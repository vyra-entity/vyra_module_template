services:
  # redis:
  #   image: redis:7
  #   container_name: redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - ./redis-data:/data
  vyra-humble:
    build: .
    volumes:
      - .:/workspace/
    stdin_open: true
    tty: true
